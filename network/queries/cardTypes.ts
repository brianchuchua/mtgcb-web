const cardTypes = `query cardTypes {
  cardTypes {
    cardTypes
    superTypes
    artifactTypes
    enchantmentTypes
    landTypes
    spellTypes
    creatureTypes
    planarTypes
    planeswalkerTypes
  }
}
`;

export default cardTypes;
